# =============================================================
# Materia Magical Staff — Docker Compose Orchestration
# Private bridge network: materia-net
# Only the app port (3000) is exposed publicly.
# Volume strategy:
#   postgres-data  → persists DB across redeployments
#   uploads-data   → persists user-uploaded images across redeployments
#                    Mounted to /app/public/uploads so Next.js serves
#                    them statically at https://{domain}/uploads/{filename}
# =============================================================

name: materia-magical-staff

networks:
  materia-net:
    driver: bridge
  dokploy-network:
    external: true   # Pre-existing Traefik network on the VPS — do not recreate

volumes:
  postgres-data:
  uploads-data:


services:
  # ── Next.js Application ─────────────────────────────────────
  app:
    build:
      context: .
      dockerfile: Dockerfile
    # No host port binding — Traefik connects via dokploy-network internally.
    # The domain → container:3000 routing is configured in Dokploy's domain settings.
    environment:
      NODE_ENV: production
      DATABASE_URL: postgresql://materia_user:${POSTGRES_PASSWORD}@postgres:5432/materia_db
    env_file:
      - .env
    volumes:
      - uploads-data:/app/public/uploads
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - materia-net
      - dokploy-network   # Traefik's network — required so Traefik can proxy to this container
    restart: unless-stopped

  # ── PostgreSQL Database ──────────────────────────────────────
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: materia_user
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: materia_db
    env_file:
      - .env
    volumes:
      - postgres-data:/var/lib/postgresql/data
    # No host port binding — postgres stays internal to the Docker network only
    networks:
      - materia-net
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U materia_user -d materia_db" ]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
